# Snake RL 项目实际依赖
# 核心依赖 (必须)
numpy>=1.24.0
torch>=2.0.0

# 数据处理 (已有，用于日志)
pandas>=2.0.0

# 可选依赖 (可视化)
# pygame>=2.5.0      # 图形界面人工玩 + Agent 可视化
# matplotlib>=3.7.0  # 训练曲线绘图
# gymnasium>=0.29.0  # 标准 RL 接口 (本项目已自实现)
